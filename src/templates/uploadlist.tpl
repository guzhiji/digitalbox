<form name="admin_upload">
    {$ListView_ListItems}
    <div align="left">{$ListView_PagingBar}</div>
    <div style="text-align: center;padding-top: 10px;">
        <input type="button" class="button1" value="{$ListView_B1_Value}" onclick="{$ListView_B1_Action}" />
        <input type="button" class="button1" value="{$ListView_B2_Value}" onclick="{$ListView_B2_Action}" />
    </div>
</form>